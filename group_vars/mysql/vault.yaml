MYSQL_ROOT_PASSWORD: "{{ (lookup('amazon.aws.aws_secret', 'roboshop/dev/mysql_root_password', region='us-east-1') | from_json).MYSQL_ROOT_PASSWORD }}" #This is secret manager store in AWS. We are using lookup plugin to fetch the value from AWS secret manager. The value is stored in JSON format, so we are using from_json filter to convert it into dictionary and then fetching the MYSQL_ROOT_PASSWORD value from it.
#"{{ lookup('amazon.aws.aws_ssm', '/roboshop/dev/mysql_root_password', region='us-east-1', decrypt=True ) }}" # This is parameter store in AWS. We are using lookup plugin to fetch the value from AWS parameter store. The value is stored in encrypted format, so we are using decrypt=True to decrypt the value before using it.
